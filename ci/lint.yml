---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: latest

inputs:
  - name: dp-cantabular-metadata-extractor

run:
  path: dp-cantabular-metadata-extractor/ci/scripts/lint.sh